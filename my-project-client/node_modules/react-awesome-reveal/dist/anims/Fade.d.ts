import * as React from 'react';
import { CommonProps, Direction } from '../const';
interface FadeOptions {
    direction?: Direction;
    reverse?: boolean;
}
export declare const Fade: React.FC<FadeOptions & CommonProps>;
export {};
