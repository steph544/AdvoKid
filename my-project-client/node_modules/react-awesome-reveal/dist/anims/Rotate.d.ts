import * as React from 'react';
import { CommonProps, RotateDirection } from '../const';
interface RotateOptions {
    direction?: RotateDirection;
    reverse?: boolean;
}
export declare const Rotate: React.FC<RotateOptions & CommonProps>;
export {};
