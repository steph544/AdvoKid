import * as React from 'react';
import { CommonProps, Direction } from '../const';
interface SlideOptions {
    direction?: Direction;
    reverse?: boolean;
}
export declare const Slide: React.FC<SlideOptions & CommonProps>;
export {};
