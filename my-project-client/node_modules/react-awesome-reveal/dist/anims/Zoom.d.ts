import * as React from 'react';
import { CommonProps, Direction } from '../const';
interface ZoomOptions {
    direction?: Direction;
    reverse?: boolean;
}
export declare const Zoom: React.FC<ZoomOptions & CommonProps>;
export {};
